import React, { useRef, useEffect } from 'react';
import { gsap } from 'gsap';
import { ScrollMagicPluginGsap, ScrollMagic } from 'scrollmagic';
import 'scrollmagic/scrollmagic/uncompressed/plugins/debug.addIndicators';

const Card = () => {
  return (
    <div>Card</div>
  )
}

export default Card